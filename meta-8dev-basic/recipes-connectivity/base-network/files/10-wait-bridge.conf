[Unit]
# hostapd uses bridge=br0, so br0 must exist first. Wait for systemd-networkd
# to create the bridge device before starting any hostapd@ instance.
Wants=sys-subsystem-net-devices-br0.device
After=sys-subsystem-net-devices-br0.device systemd-networkd.service
