# Succeed when ANY managed link is online. The default waits for ALL links, but
# an unplugged wired port stays "no-carrier" forever and times out the unit.
[Service]
ExecStart=
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --any --timeout=10
